<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Workflows</title>
    <!-- External stylesheet link -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f9;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #343a40;
        }

        .btn-create-workflow {
            margin: 20px 0;
            font-size: 1rem;
            background-color: #007bff;
            color: white;
            padding: 10px 15px;
            border-radius: 5px;
            text-decoration: none;
        }

        .btn-create-workflow:hover {
            background-color: #0056b3;
        }

        .table-wrapper {
            margin-top: 30px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table th, table td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        table th {
            background-color: #007bff;
            color: white;
        }

        table tr:hover {
            background-color: #f1f1f1;
        }

        .view-button {
            background-color: #28a745;
            color: white;
            padding: 6px 12px;
            border-radius: 5px;
            text-decoration: none;
        }

        .view-button:hover {
            background-color: #218838;
        }

        .no-workflows {
            font-size: 1.25rem;
            color: #888;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>All Workflows</h1>

        <!-- Button to create a new workflow (you can replace this link with your form to create workflows) -->
        <a href="/create" class="btn-create-workflow">
            <i class="fas fa-plus"></i> Create New Workflow
        </a>

        <!-- Table displaying workflows -->
        <div class="table-wrapper">
            <% if (workflows && workflows.length > 0) { %>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Workflow Name</th>
                            <th>Created At</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% workflows.forEach(workflow => { %>
                            <tr>
                                <td><%= workflow.name %></td>
                                <td><%= workflow.created_at %></td>
                                <td>
                                    <a href="/leads/viewleads/<%= workflow.id %>" class="view-button">View Leads</a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <p class="no-workflows">No workflows available.</p>
            <% } %>
        </div>
    </div>
    
    <!-- Bootstrap JS for potential future functionality (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
